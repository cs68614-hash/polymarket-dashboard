// Dashboard config (client-side)
export const CONFIG = {
  // Alert thresholds
  thresholds: {
    spread: 0.02,
    absMove1h: 0.005, // 0.5%
    vol24h: 2000,
    daysToEnd: 14
  },
  // Panel sizes
  maxAlerts: 12,
  maxTableRows: 50
};
